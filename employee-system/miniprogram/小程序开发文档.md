# 员工管理系统小程序开发文档

## 1. 项目概述

本项目是员工管理系统的微信小程序端，基于 uni-app、Vue 3、TypeScript 开发，运行目标为微信小程序。

项目目录：

```text
D:/xiangmu/cainiao/employee-system/
├── backend/       后端 Spring Boot 服务
├── frontend/      Web 前端
└── miniprogram/   微信小程序端
```

小程序目录必须打开：

```text
D:/xiangmu/cainiao/employee-system/miniprogram
```

不要只单独打开 `README.md` 或某个 `.vue` 文件，否则 HBuilderX 会认为这是非项目文件，无法运行到小程序模拟器。

## 2. 技术栈

| 类型 | 技术 |
| --- | --- |
| 框架 | uni-app |
| 前端框架 | Vue 3 |
| 语言 | TypeScript |
| 状态管理 | Pinia |
| 日期处理 | dayjs |
| 样式 | SCSS |
| 小程序平台 | 微信小程序 |
| 后端 | Spring Boot |
| 数据库 | MySQL |

## 3. 环境准备

需要安装：

1. HBuilderX
2. 微信开发者工具
3. Node.js
4. Java / Maven
5. MySQL

微信开发者工具需要开启服务端口：

```text
微信开发者工具 -> 设置 -> 安全设置 -> 服务端口 -> 开启
```

本地开发时还需要关闭合法域名校验：

```text
微信开发者工具 -> 详情 -> 本地设置 -> 勾选“不校验合法域名、web-view、TLS版本以及HTTPS证书”
```

项目配置中也已经关闭：

```json
{
  "setting": {
    "urlCheck": false
  }
}
```

相关文件：

```text
miniprogram/project.config.json
miniprogram/project.private.config.json
miniprogram/manifest.json
```

## 4. 安装依赖

进入小程序目录：

```powershell
cd D:/xiangmu/cainiao/employee-system/miniprogram
npm install
```

常用命令：

```powershell
npm run dev:mp-weixin
npm run build:mp-weixin
npm run dev:h5
npm run build:h5
```

## 5. 后端启动

后端默认端口：

```text
8080
```

配置文件：

```text
backend/src/main/resources/application.yml
```

数据库配置：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: zc123456
```

启动后端：

```powershell
cd D:/xiangmu/cainiao/employee-system/backend
mvn spring-boot:run
```

也可以在项目根目录执行：

```powershell
./start-backend.ps1
```

验证登录接口：

```powershell
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8080/auth/login -ContentType 'application/json' -Body '{"username":"admin","password":"admin123"}'
```

默认账号：

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 管理员 | admin | admin123 |

## 6. HBuilderX 运行小程序

正确方式：

1. HBuilderX 打开 `D:/xiangmu/cainiao/employee-system/miniprogram`
2. 点击左侧项目根目录 `miniprogram`，或打开 `App.vue`
3. 点击：

```text
运行 -> 运行到小程序模拟器 -> 微信开发者工具
```

HBuilderX 会编译到：

```text
miniprogram/unpackage/dist/dev/mp-weixin
```

如果微信开发者工具打开后停在项目列表，手动双击 `mp-weixin` 项目即可。编译成功但没有自动进入项目，通常是微信开发者工具 CLI 自动打开不稳定，不是代码编译失败。

## 7. 微信开发者工具导入

如果不用 HBuilderX 自动打开，也可以手动导入生成目录：

开发模式目录：

```text
D:/xiangmu/cainiao/employee-system/miniprogram/unpackage/dist/dev/mp-weixin
```

命令行发行构建目录：

```text
D:/xiangmu/cainiao/employee-system/miniprogram/dist/build/mp-weixin
```

注意：不要导入 `miniprogram` 源码目录到微信开发者工具运行。微信开发者工具应该打开 uni-app 编译后的 `mp-weixin` 目录。

## 8. 请求地址说明

请求封装文件：

```text
miniprogram/utils/request.ts
```

当前请求地址策略：

```ts
const DEVTOOLS_BASE_URL = 'http://127.0.0.1:8080'
const DEVICE_BASE_URL = 'http://192.168.31.139:8080'
```

规则：

| 运行环境 | 请求地址 |
| --- | --- |
| 微信开发者工具模拟器 | `http://127.0.0.1:8080` |
| 真机调试 | `http://192.168.31.139:8080` |

原因：

1. 开发者工具模拟器运行在电脑上，可以访问电脑本机的 `127.0.0.1`。
2. 真机运行在手机上，手机里的 `127.0.0.1` 指手机自己，不能访问电脑后端。
3. 真机必须通过电脑的局域网 IP 访问后端，例如 `192.168.31.139`。

如果电脑 IP 变化，需要修改：

```text
miniprogram/utils/request.ts
```

把 `DEVICE_BASE_URL` 改成新的电脑局域网 IP。

查看电脑 IP：

```powershell
ipconfig
```

一般看无线网卡 IPv4 地址。

## 9. 页面结构

主 Tab 页面：

| 页面 | 路径 | 说明 |
| --- | --- | --- |
| 首页 | `pages/home/index` | 首页统计、公告、快捷入口 |
| 考勤 | `pages/attendance/index` | 打卡、考勤记录 |
| 申请 | `pages/apply/index` | 请假、报销、日报入口 |
| 我的 | `pages/mine/index` | 用户信息、退出登录 |

业务页面：

| 页面 | 路径 |
| --- | --- |
| 登录 | `pages/login/index` |
| 请假申请 | `pages/leave/apply` |
| 请假详情 | `pages/leave/detail` |
| 报销申请 | `pages/reimbursement/apply` |
| 报销详情 | `pages/reimbursement/detail` |
| 提交日报 | `pages/dailyReport/submit` |
| 日报详情 | `pages/dailyReport/detail` |
| 公告详情 | `pages/announcement/detail` |
| 工资条 | `pages/salary/detail` |

页面配置文件：

```text
miniprogram/pages.json
```

新增页面后必须在 `pages.json` 中注册，否则微信小程序无法跳转。

## 10. 目录说明

```text
miniprogram/
├── api/             接口模块
├── pages/           页面
├── store/           Pinia 状态
├── styles/          公共样式
├── types/           TypeScript 类型
├── utils/           工具函数、请求封装
├── App.vue          应用入口
├── main.ts          Vue 初始化
├── pages.json       页面和 tabBar 配置
├── manifest.json    平台配置
├── project.config.json
└── project.private.config.json
```

接口模块按业务拆分：

```text
api/auth.ts
api/attendance.ts
api/announcement.ts
api/leave.ts
api/reimbursement.ts
api/dailyReport.ts
api/salary.ts
```

## 11. 登录和 Token

登录接口：

```text
POST /auth/login
```

请求体：

```json
{
  "username": "admin",
  "password": "admin123"
}
```

登录成功后返回 JWT Token，小程序会保存：

```text
token
userInfo
```

请求封装会自动加请求头：

```text
Authorization: Bearer <token>
```

如果接口返回 401，小程序会清理登录信息并跳转登录页。

## 12. 常见问题

### 12.1 HBuilderX 提示 Markdown 文件不能运行

原因：当前选中的是 `README.md` 或单独打开的文件，不是项目。

处理：

1. 点击左侧项目根目录 `miniprogram`
2. 或打开 `App.vue`
3. 再点击运行到微信小程序

### 12.2 HBuilderX 启动微信开发者工具失败

报错类似：

```text
IDE service port disabled
```

处理：

```text
微信开发者工具 -> 设置 -> 安全设置 -> 服务端口 -> 开启
```

然后关闭微信开发者工具，重新从 HBuilderX 运行。

### 12.3 微信开发者工具打开后是空白或停在项目列表

处理：

1. 关闭所有微信开发者工具窗口
2. 从 HBuilderX 重新运行
3. 如果仍停在项目列表，手动双击 `mp-weixin`

### 12.4 `request:fail net::ERR_CONNECTION_REFUSED`

常见原因：

1. 后端没有启动
2. 端口不是 8080
3. 真机请求了 `127.0.0.1`
4. 手机和电脑不在同一个 Wi-Fi
5. Windows 防火墙拦截了 8080

检查后端：

```powershell
netstat -ano | Select-String ':8080'
```

本机测试：

```powershell
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8080/auth/login -ContentType 'application/json' -Body '{"username":"admin","password":"admin123"}'
```

局域网测试：

```powershell
Invoke-RestMethod -Method Post -Uri http://192.168.31.139:8080/auth/login -ContentType 'application/json' -Body '{"username":"admin","password":"admin123"}'
```

### 12.5 `request:fail url not in domain list`

原因：微信开发者工具开启了合法域名校验，本地 IP 或 HTTP 地址被拦截。

处理：

```text
微信开发者工具 -> 详情 -> 本地设置 -> 勾选“不校验合法域名、web-view、TLS版本以及HTTPS证书”
```

同时确认：

```text
project.config.json -> setting.urlCheck = false
project.private.config.json -> setting.urlCheck = false
```

### 12.6 本地模拟器请求 `192.168.31.139` 出现 502

原因：微信开发者工具本地调试代理转发局域网 IP 时可能返回 `502 Bad Gateway`。

处理：本地模拟器使用 `127.0.0.1`，真机使用局域网 IP。当前项目已在 `utils/request.ts` 中自动判断。

### 12.7 修改代码后真机没有更新

处理：

1. HBuilderX 停止运行
2. 关闭微信开发者工具项目窗口
3. 重新运行到微信开发者工具
4. 查看控制台 `[Request] URL:` 是否是最新地址

## 13. 开发规范

### 页面开发

1. 页面放在 `pages/` 下。
2. 新页面必须注册到 `pages.json`。
3. 页面跳转使用 `uni.navigateTo`、`uni.switchTab`、`uni.reLaunch`。
4. Tab 页面只能使用 `uni.switchTab` 跳转。

### 接口开发

1. 接口统一写在 `api/` 目录。
2. 不要在页面里直接写 `uni.request`。
3. 请求统一走 `utils/request.ts`。
4. 需要登录的接口由请求封装自动带 Token。

### 状态管理

登录状态放在：

```text
store/auth.ts
```

页面需要用户信息时优先从 auth store 获取。

### 样式开发

公共样式放在：

```text
app.scss
styles/
uni.scss
```

页面样式写在当前 `.vue` 文件的 `<style lang="scss">` 中。

## 14. 构建和发布

开发构建：

```powershell
npm run dev:mp-weixin
```

发行构建：

```powershell
npm run build:mp-weixin
```

发行构建输出：

```text
miniprogram/dist/build/mp-weixin
```

发布前注意：

1. 后端接口不能使用本地 IP。
2. 微信公众平台需要配置正式 HTTPS 合法域名。
3. `urlCheck` 只适合本地开发关闭，正式发布不能依赖关闭校验。
4. 检查 `manifest.json` 中的 appid。
5. 使用微信开发者工具上传代码。

## 15. 快速启动清单

1. 启动 MySQL。
2. 启动后端：

```powershell
cd D:/xiangmu/cainiao/employee-system/backend
mvn spring-boot:run
```

3. 打开 HBuilderX。
4. 打开项目目录：

```text
D:/xiangmu/cainiao/employee-system/miniprogram
```

5. 运行到微信小程序模拟器。
6. 登录：

```text
admin / admin123
```

7. 如果是真机调试，确认手机和电脑在同一 Wi-Fi，并检查 `DEVICE_BASE_URL` 是否是当前电脑 IP。

# 员工管理系统 - 7天学习日报

## 📋 项目概述

| 模块 | 技术栈 | 说明 |
|------|--------|------|
| **后端** | Spring Boot 2.7 + MyBatis Plus 3.5 | RESTful API、数据持久化 |
| **前端** | Vue 3 + Vite 4 + Ant Design Vue 4 | 响应式UI、路由管理 |
| **数据库** | MySQL 8.0 | 关系型数据存储 |
| **安全** | Spring Security + JWT | 身份认证与授权 |

---

## 📁 项目目录结构

```
employee-system/
├── backend/                    # 后端服务
│   ├── src/main/java/com/example/employee/
│   │   ├── controller/         # REST API控制层
│   │   │   ├── AuthController.java      # 认证接口
│   │   │   ├── EmployeeController.java  # 员工管理接口
│   │   │   ├── DepartmentController.java# 部门管理接口
│   │   │   ├── AttendanceController.java# 考勤管理接口
│   │   │   ├── AnnouncementController.java # 公告管理接口
│   │   │   ├── LeaveController.java     # 请假管理接口
│   │   │   └── UserController.java      # 用户账号管理接口
│   │   ├── service/            # 业务逻辑层
│   │   │   └── UserService.java          # 用户服务接口
│   │   ├── service/impl/       # 业务逻辑实现
│   │   │   └── UserServiceImpl.java      # 用户服务实现
│   │   ├── mapper/             # 数据访问层
│   │   ├── entity/             # 实体类
│   │   │   └── User.java                  # 用户实体(含role字段)
│   │   ├── config/             # 配置类
│   │   │   ├── SecurityConfig.java        # 安全配置
│   │   │   ├── JwtAuthenticationFilter.java # JWT过滤器
│   │   │   └── DataInitializer.java       # 数据初始化器
│   │   └── util/               # 工具类
│   └── src/main/resources/
└── frontend/                   # 前端页面
    ├── src/
    │   ├── views/
    │   │   ├── Login.vue                 # 登录页面
    │   │   ├── Layout.vue                # 主布局(含侧边栏菜单)
    │   │   ├── Dashboard.vue             # 首页仪表盘
    │   │   ├── employees/               # 员工管理页面
    │   │   ├── departments/             # 部门管理页面
    │   │   ├── attendance/              # 考勤管理页面
    │   │   ├── announcements/           # 公告通知页面
    │   │   ├── leaves/                  # 请假管理页面
    │   │   └── users/                   # 账号管理页面
    │   │       ├── UserList.vue         # 用户列表页
    │   │       └── UserForm.vue         # 用户新增/编辑表单
    │   ├── api/                         # API封装层
    │   │   ├── auth.js                  # 认证API(含拦截器)
    │   │   ├── employee.js              # 员工API(含拦截器)
    │   │   ├── user.js                  # 用户管理API
    │   │   └── ...
    │   ├── store/                       # Pinia状态管理
    │   │   ├── auth.js                  # 认证状态(含角色)
    │   │   └── user.js                  # 用户管理状态
    │   └── router/index.js              # 路由配置(含权限)
    └── start-backend.ps1                # 一键启动脚本
```

---

## 📖 第1天：项目概述与环境搭建

### 学习目标
- 了解项目整体架构
- 搭建开发环境(JDK 1.8.0_201 + Node.js)
- 配置MySQL数据库
- 解决IDEA运行配置问题

### 技术栈说明

#### 后端核心技术
- **Spring Boot 2.7.0**：简化Spring应用开发，提供自动配置
- **MyBatis Plus 3.5.2**：增强MyBatis，简化CRUD操作
- **Spring Security**：安全框架，提供认证和授权
- **JWT (jjwt 0.9.1)**：无状态认证机制
- **Swagger 3.0**：API文档生成
- **Lombok**：简化实体类代码
- **BCryptPasswordEncoder**：密码加密存储

#### 前端核心技术
- **Vue 3.3.0**：渐进式JavaScript框架(Composition API)
- **Vite 4.5.0**：新一代前端构建工具(HMR热更新)
- **Ant Design Vue 4.0.0**：企业级UI组件库
- **Pinia 2.1.0**：Vue3状态管理库
- **Vue Router 4.2.0**：Vue官方路由管理(导航守卫)
- **Axios 1.6.0**：HTTP请求库(请求/响应拦截器)

### 环境搭建步骤

#### 1. JDK环境配置
```bash
# JDK安装路径
C:\Program Files\Java\jdk1.8.0_201

# 验证安装
java -version
```

#### 2. 数据库初始化
```sql
CREATE DATABASE IF NOT EXISTS employee_db DEFAULT CHARACTER SET utf8mb4;
```

#### 3. 项目依赖安装
```bash
# 后端Maven自动下载依赖
cd backend
mvn clean install

# 前端npm安装
cd frontend
npm install
```

#### 4. 启动项目
```bash
# 方式1: 使用一键启动脚本(推荐)
powershell -ExecutionPolicy Bypass -File start-backend.ps1

# 方式2: IDEA中点击运行按钮
# 注意: 需要选择JDK 1.8.0_201作为运行时JDK

# 前端启动
cd frontend
npm run dev
```

### 常见问题解决

#### 端口占用问题
**问题：** `Port 8080 is already in use`
**解决方案：**
```powershell
# 查找占用进程
Get-NetTCPConnection -LocalPort 8080

# 终止进程
taskkill /F /PID <进程ID>
```

**自动化方案：** 在`EmployeeApplication.main()`中添加自动释放端口逻辑：
```java
public static void main(String[] args) {
    freePort(8080);  // 启动前自动释放端口
    SpringApplication.run(EmployeeApplication.class, args);
}
```

#### IDEA启动按钮灰色
**可能原因：**
1. Maven索引未完成 → 等待进度条完成
2. 运行配置缺失 → 右键主类 → Run 'EmployeeApplication'
3. JDK版本不匹配 → File → Project Structure → SDKs → 选择JDK 1.8

---

## 📖 第2天：后端核心架构

### 学习目标
- 理解Spring Boot启动原理
- 掌握MyBatis Plus配置与分页插件
- 学会统一响应封装(Result)和全局异常处理

### 核心配置详解

#### 1. Spring Boot启动类(增强版)

**文件：** `EmployeeApplication.java`

```java
@SpringBootApplication
@MapperScan("com.example.employee.mapper")
public class EmployeeApplication {

    public static void main(String[] args) {
        freePort(8080);  // 自动释放端口
        SpringApplication.run(EmployeeApplication.class, args);
    }

    private static void freePort(int port) {
        try {
            ProcessBuilder pb = new ProcessBuilder("powershell", "-Command",
                "(Get-NetTCPConnection -LocalPort " + port +
                " -ErrorAction SilentlyContinue).OwningProcess " +
                "| ForEach-Object { taskkill /F /PID $_ }");
            Process process = pb.start();
            process.waitFor();
            Thread.sleep(1500);  // 等待端口完全释放
        } catch (Exception e) {
            System.out.println(">>> Port check skipped");
        }
    }
}
```

#### 2. MyBatis Plus配置

**application.yml关键配置：**
```yaml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true  # 驼峰命名转换
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # SQL日志
  global-config:
    db-config:
      id-type: auto  # 主键自增
```

#### 3. 统一响应封装

**文件：** `common/Result.java`

```java
@Data
public class Result<T> {
    private Integer code;      // 200成功, 500失败
    private String message;    // 提示信息
    private T data;           // 响应数据

    public static <T> Result<T> success(T data) { ... }
    public static <T> Result<T> error(String msg) { ... }
}
```

**响应格式示例：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

#### 4. 全局异常处理

**文件：** `GlobalExceptionHandler.java`

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<?> handleValidException(MethodArgumentNotValidException e) { ... }

    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) { ... }

    @ExceptionHandler(Exception.class)
    public Result<?> handleGeneric(Exception e) { ... }
}
```

#### 5. 数据初始化器

**文件：** `config/DataInitializer.java`

**功能：**
- 自动添加`role`字段到user表
- 初始化管理员账号(admin/admin123)

```java
@Component
public class DataInitializer implements CommandLineRunner {

    @Override
    public void run(String... args) throws Exception {
        try {
            jdbcTemplate.execute(
                "ALTER TABLE `user` ADD COLUMN `role` varchar(20) " +
                "NOT NULL DEFAULT 'employee' COMMENT '角色' AFTER `real_name`"
            );
        } catch (Exception ignored) {}

        User admin = userMapper.selectById(1L);
        if (admin == null) {
            admin = new User();
            admin.setUsername("admin");
            admin.setPassword(passwordEncoder.encode("admin123"));
            admin.setRealName("系统管理员");
            admin.setRole("admin");
            userMapper.insert(admin);
        } else {
            admin.setPassword(passwordEncoder.encode("admin123"));
            admin.setRole("admin");
            userMapper.updateById(admin);
        }
    }
}
```

---

## 📖 第3天：员工管理模块(CRUD完整实现)

### 学习目标
- 掌握RESTful API设计规范
- 实现员工管理完整CRUD功能
- 理解MyBatis Plus的Service层继承(IService)

### 数据库表设计

**员工表(employee)：**

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | bigint(20) | 员工ID | 主键自增 |
| name | varchar(50) | 姓名 | 非空 |
| age | int(11) | 年龄 | 18-100 |
| gender | varchar(10) | 性别 | 男/女 |
| phone | varchar(20) | 电话 | 手机号格式 |
| email | varchar(100) | 邮箱 | 邮箱格式 |
| department | varchar(50) | 部门 | 非空 |
| position | varchar(50) | 职位 | 非空 |
| hire_date | date | 入职日期 | 非空 |

### 实体类设计

**文件：** `entity/Employee.java`

```java
@Data
@TableName("employee")
@ApiModel(description = "员工实体")
public class Employee implements Serializable {
    @TableId(type = IdType.AUTO)
    private Long id;

    @NotBlank(message = "姓名不能为空")
    private String name;

    @NotNull @Min(18) @Max(100)
    private Integer age;

    @NotBlank
    private String gender;

    @Pattern(regexp = "^1[3-9]\\d{9}$")
    private String phone;

    @Email
    private String email;

    @NotBlank
    private String department, position, hireDate;
}
```

### Controller层(RESTful API)

**文件：** `controller/EmployeeController.java`

| 接口路径 | 方法 | 功能 | 参数 |
|----------|------|------|------|
| `/employees` | GET | 分页查询 | page, pageSize, keyword |
| `/employees/{id}` | GET | 详情查询 | id |
| `/employees` | POST | 新增员工 | Employee JSON |
| `/employees/{id}` | PUT | 更新员工 | id, Employee JSON |
| `/employees/{id}` | DELETE | 删除员工 | id |

### Service层(MyBatis Plus IService)

**关键点：** 继承`IService<Entity>`获得内置CRUD方法：
- `getById(id)` - 根据ID查询
- `save(entity)` - 新增
- `updateById(entity)` - 更新
- `removeById(id)` - 删除
- `page(page, wrapper)` - 分页查询

```java
@Service
public class EmployeeServiceImpl extends ServiceImpl<EmployeeMapper, Employee>
        implements EmployeeService {

    @Override
    public IPage<Employee> getEmployeeList(Integer page, Integer pageSize, String keyword) {
        Page<Employee> pageObj = new Page<>(page, pageSize);
        QueryWrapper<Employee> wrapper = new QueryWrapper<>();

        if (StringUtils.isNotBlank(keyword)) {
            wrapper.like("name", keyword)
                   .or().like("department", keyword)
                   .or().like("position", keyword);
        }

        return page(pageObj, wrapper);
    }
}
```

---

## 📖 第4天：用户认证与JWT安全机制

### 学习目标
- 理解JWT无状态认证原理
- 掌握Spring Security配置
- 实现前后端Token交互

### 认证流程图

```
┌─────────┐     ┌─────────────┐     ┌──────────────┐
│  前端    │────▶│  后端验证    │────▶│  生成JWT Token │
│ 登录表单 │◀────│  用户名密码  │◀────│  返回给前端    │
└─────────┘     └─────────────┘     └──────────────┘
                                            │
                    ┌────────────────────────┘
                    ▼
┌─────────┐     ┌─────────────┐     ┌──────────────┐
│  后端    │◀────│  JWT过滤器   │◀────│  携带Token    │
│ 返回数据 │────▶│  验证Token  │────▶│  每次请求Header│
└─────────┘     └─────────────┘     └──────────────┘
```

### 用户表设计(user)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint(20) | 用户ID |
| username | varchar(50) | 用户名(唯一) |
| password | varchar(100) | BCrypt加密密码 |
| real_name | varchar(50) | 真实姓名 |
| role | varchar(20) | 角色(admin/employee) |

**测试账号：**
- 用户名：`admin`
- 密码：`admin123`
- 角色：管理员

### JWT工具类

**文件：** `util/JwtUtil.java`

```java
@Component
public class JwtUtil {

    @Value("${jwt.secret}")
    private String secret;  // 密钥

    @Value("${jwt.expiration}")
    private Long expiration;  // 过期时间(ms)

    public String generateToken(String username) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);

        return Jwts.builder()
                .setSubject(username)
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(SignatureAlgorithm.HS512, secret)
                .compact();
    }

    public boolean validateToken(String token) {
        try {
            Jwts.parser().setSigningKey(secret).parseClaimsJws(token);
            return true;
        } catch (JwtException e) {
            return false;
        }
    }
}
```

### JWT过滤器链

**Security过滤链顺序：**
```
CorsFilter → LogoutFilter → JwtAuthenticationFilter →
UsernamePasswordAuthFilter → SessionManagementFilter → ...
```

**文件：** `config/JwtAuthenticationFilter.java`

```java
@Component
@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtUtil jwtUtil;
    private final UserDetailsService userDetailsService;

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                    HttpServletResponse response,
                                    FilterChain chain) throws Exception {
        String authHeader = request.getHeader("Authorization");

        if (authHeader != null && authHeader.startsWith("Bearer ")) {
            String token = authHeader.substring(7);
            String username = jwtUtil.getUsernameFromToken(token);

            if (username != null &&
                SecurityContextHolder.getContext().getAuthentication() == null) {

                UserDetails userDetails =
                    userDetailsService.loadUserByUsername(username);

                if (jwtUtil.validateToken(token)) {
                    UsernamePasswordAuthenticationToken auth =
                        new UsernamePasswordAuthenticationToken(
                            userDetails, null, userDetails.getAuthorities());
                    SecurityContextHolder.getContext().setAuthentication(auth);
                }
            }
        }

        chain.doFilter(request, response);
    }
}
```

### Security配置

**文件：** `config/SecurityConfig.java`

```java
@Configuration
@EnableWebSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf().disable()
            .authorizeRequests()
                .antMatchers("/auth/**").permitAll()  // 认证接口公开
                .antMatchers("/swagger-ui/**").permitAll()
                .anyRequest().authenticated()  // 其他需要认证
            .and()
            .sessionManagement()
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)  // 无状态
            .and()
            .addFilterBefore(jwtFilter,
                UsernamePasswordAuthenticationFilter.class);  // JWT过滤器位置

        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();  // BCrypt加密
    }
}
```

### 认证接口(AuthController)

| 接口 | 方法 | 功能 | 请求参数 |
|------|------|------|----------|
| `/api/auth/login` | POST | 用户登录 | {username, password} |

**登录响应：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "userId": 1,
    "role": "admin",
    "realName": "系统管理员"
  }
}
```

---

## 📖 第5天：部门管理与考勤系统

### 学习目标
- 掌握多模块业务开发模式
- 理解业务规则封装
- 学会时间处理和状态判断

### 部门管理模块

**部门表(department)：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint(20) | 部门ID |
| name | varchar(50) | 部门名称 |
| manager | varchar(50) | 负责人 |
| phone | varchar(20) | 联系电话 |
| description | varchar(200) | 部门描述 |
| status | tinyint(1) | 状态(0正常/1停用) |

**API接口：**

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/departments` | 获取部门列表 |
| POST | `/departments` | 添加部门 |
| PUT | `/departments/{id}` | 更新部门 |
| DELETE | `/departments/{id}` | 删除部门 |

### 考勤管理模块

**考勤表(attendance)：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint(20) | 考勤ID |
| employee_id | bigint(20) | 员工ID |
| employee_name | varchar(50) | 员工姓名 |
| date | date | 考勤日期 |
| check_in_time | datetime | 签到时间 |
| check_out_time | datetime | 签退时间 |
| work_hours | decimal(4,1) | 工作时长 |
| status | varchar(20) | 状态 |

**考勤状态判断规则：**

| 状态 | 条件 |
|------|------|
| 正常 | 签到≤9:00 且 签退≥18:00 |
| 迟到 | 签到>9:00 |
| 早退 | 签退<18:00 |
| 缺卡 | 未签到或未签退 |

**核心API：**

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/attendance` | 考勤记录列表(支持日期筛选) |
| POST | `/attendance/checkin` | 签到打卡 |
| POST | `/attendance/checkout` | 签退打卡 |

---

## 📖 第6天：公告通知与请假管理

### 学习目标
- 实现信息发布功能
- 掌握审批流程设计
- 学会状态机管理

### 公告通知模块

**公告表(announcement)：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint(20) | 公告ID |
| title | varchar(100) | 标题 |
| content | text | 内容 |
| publisher | varchar(50) | 发布人 |
| type | varchar(20) | 类型(通知/公告/紧急) |
| status | tinyint(1) | 状态(0草稿/1已发布) |
| publish_time | datetime | 发布时间 |

### 请假管理模块

**请假表(leave_request)：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint(20) | 请假ID |
| employee_id | bigint(20) | 员工ID |
| leave_type | varchar(20) | 类型(事假/病假/年假/调休) |
| start_date | date | 开始日期 |
| end_date | date | 结束日期 |
| days | int | 天数 |
| reason | varchar(500) | 请假原因 |
| status | varchar(20) | 状态 |
| approver_id | bigint(20) | 审批人ID |

**请假状态流转：**
```
待审批(PENDING) ──▶ 已批准(APPROVED)
       │
       └──▶ 已拒绝(REJECTED)
```

**请假API：**

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/leaves` | 请假列表(按角色筛选) |
| POST | `/leaves` | 提交请假申请 |
| PUT | `/leaves/{id}/approve` | 审批通过 |
| PUT | `/leaves/{id}/reject` | 审批拒绝 |
| DELETE | `/leaves/{id}` | 删除请假 |

---

## 📖 第7天：账号管理与权限控制系统(重点)

### 学习目标
- 实现完整的RBAC角色权限控制
- 开发用户账号管理CRUD功能
- 配置前端路由守卫与菜单权限
- 实现API请求/响应拦截器

### RBAC权限模型设计

```
┌─────────────────────────────────────────────────────┐
│                   权限控制架构                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│   ┌──────────┐         ┌─────────────────────────┐  │
│   │  Admin   │         │       Employee          │  │
│   │  管理员  │         │       普通员工          │  │
│   └────┬─────┘         └───────────┬─────────────┘  │
│        │                           │                │
│        ▼                           ▼                │
│   ┌─────────────────────────────────────────┐       │
│   │              可访问菜单                  │       │
│   ├─────────────────────────────────────────┤       │
│   │ ✓ 首页概览     │  ✓ 首页概览              │       │
│   │ ✓ 员工管理     │  ✗ 员工管理(隐藏)        │       │
│   │ ✓ 部门管理     │  ✗ 部门管理(隐藏)        │       │
│   │ ✓ 考勤管理     │  ✓ 考勤管理              │       │
│   │ ✓ 公告通知     │  ✓ 公告通知              │       │
│   │ ✓ 请假管理     │  ✓ 请假管理              │       │
│   │ ✓ 账号管理     │  ✗ 账号管理(隐藏)        │       │
│   └─────────────────────────────────────────┘       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 后端：用户账号管理

#### 1. User实体类(含角色字段)

**文件：** `entity/User.java`

```java
@Data
@TableName("user")
public class User implements Serializable {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String username;      // 用户名
    private String password;      // BCrypt密码
    private String realName;      // 真实姓名
    private String role;          // 角色: admin / employee
}
```

#### 2. UserService接口(继承IService)

**文件：** `service/UserService.java`

```java
public interface UserService extends IService<User> {
    IPage<User> getUserList(Integer page, Integer pageSize, String keyword);
    User getUserByUsername(String username);
}
```

**关键点：** 继承`IService<User>`获得MyBatis Plus内置方法：
- `getById(id)`, `save(entity)`, `updateById(entity)`, `removeById(id)`
- `page(page, wrapper)` 分页查询

#### 3. UserServiceImpl实现

**文件：** `service/impl/UserServiceImpl.java`

```java
@Service
@RequiredArgsConstructor
public class UserServiceImpl extends ServiceImpl<UserMapper, User>
        implements UserService {

    @Override
    public IPage<User> getUserList(Integer page, Integer pageSize, String keyword) {
        Page<User> pageObj = new Page<>(page, pageSize);
        QueryWrapper<User> wrapper = new QueryWrapper<>();

        if (StringUtils.isNotBlank(keyword)) {
            wrapper.like("username", keyword)
                   .or().like("real_name", keyword)
                   .or().like("role", keyword);
        }

        wrapper.orderByDesc("id");
        return page(pageObj, wrapper);
    }

    @Override
    public User getUserByUsername(String username) {
        return getOne(new QueryWrapper<User>().eq("username", username));
    }
}
```

#### 4. UserController控制器

**文件：** `controller/UserController.java`

**完整API清单：**

| 方法 | 路径 | 功能 | 权限 |
|------|------|------|------|
| GET | `/users` | 用户列表(分页+搜索) | Admin |
| GET | `/users/{id}` | 用户详情 | Admin |
| POST | `/users` | 添加用户 | Admin |
| PUT | `/users/{id}` | 更新用户 | Admin |
| DELETE | `/users/{id}` | 删除用户 | Admin |
| PUT | `/users/{id}/reset-password` | 重置密码为123456 | Admin |

**核心代码示例：**

```java
@Api(tags = "账号管理")
@RestController
@RequestMapping("/users")
@RequiredArgsConstructor
public class UserController {

    private final UserService userService;
    private final PasswordEncoder passwordEncoder;

    @ApiOperation("添加用户")
    @PostMapping
    public Result<User> addUser(@Valid @RequestBody User user) {
        User existUser = userService.getUserByUsername(user.getUsername());
        if (existUser != null) {
            return Result.error("用户名已存在");
        }
        user.setPassword(passwordEncoder.encode(user.getPassword()));
        boolean success = userService.save(user);
        if (success) {
            user.setPassword(null);  // 不返回密码
            return Result.success(user);
        }
        return Result.error("添加失败");
    }

    @ApiOperation("重置密码")
    @PutMapping("/{id}/reset-password")
    public Result<Boolean> resetPassword(@PathVariable Long id) {
        User user = userService.getById(id);
        if (user == null) {
            return Result.error("用户不存在");
        }
        user.setPassword(passwordEncoder.encode("123456"));  // 重置为默认密码
        boolean success = userService.updateById(user);
        return success ? Result.success(true) : Result.error("重置失败");
    }
}
```

### 前端：权限控制系统

#### 1. Auth Store(认证状态管理)

**文件：** `store/auth.js`

```javascript
import { defineStore } from 'pinia'

export const useAuthStore = defineStore('auth', {
  state: () => ({
    token: localStorage.getItem('token') || '',
    username: localStorage.getItem('username') || '',
    userId: localStorage.getItem('userId') || '',
    role: localStorage.getItem('role') || '',       // 角色字段
    realName: localStorage.getItem('realName') || ''
  }),

  getters: {
    isLoggedIn: (state) => !!state.token,
    isAdmin: (state) => state.role === 'admin'       // 管理员判断
  },

  actions: {
    async login(credentials) {
      const res = await loginApi(credentials)
      const data = res.data.data

      this.token = data.token
      this.role = data.role
      this.userId = data.userId
      this.realName = data.realName

      localStorage.setItem('token', data.token)
      localStorage.setItem('role', data.role)
      // ... 其他存储
    },

    logout() {
      this.token = ''
      this.role = ''
      localStorage.clear()
    }
  }
})
```

#### 2. Router路由守卫(权限控制)

**文件：** `router/index.js`

```javascript
const routes = [
  // ... 其他路由
  {
    path: '/users',
    component: () => import('../views/users/UserList.vue'),
    meta: { title: '账号管理', roles: ['admin'] }  // 仅管理员可访问
  },
  {
    path: '/users/add',
    component: () => import('../views/users/UserForm.vue'),
    meta: { title: '添加账号', roles: ['admin'] }
  }
]

router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  // 未登录跳转登录页
  if (to.meta.requiresAuth !== false && !authStore.isLoggedIn) {
    return next('/login')
  }

  // 已登录访问登录页，跳转首页
  if (to.path === '/login' && authStore.isLoggedIn) {
    return next('/dashboard')
  }

  // 角色权限检查
  if (to.meta.roles && !to.meta.roles.includes(authStore.role)) {
    return next('/dashboard')  // 无权限回首页
  }

  next()
})
```

#### 3. Layout侧边栏(动态菜单)

**文件：** `views/Layout.vue`

```vue
<a-menu>
  <!-- 所有角色可见 -->
  <a-menu-item key="dashboard">首页概览</a-menu-item>

  <!-- 仅管理员可见 -->
  <a-sub-menu v-if="authStore.isAdmin" key="emp-sub">
    <template #title>员工管理</template>
    <a-menu-item key="employees">员工列表</a-menu-item>
    <a-menu-item key="add">添加员工</a-menu-item>
  </a-sub-menu>

  <a-sub-menu v-if="authStore.isAdmin" key="dept-sub">
    <template #title>部门管理</template>
    <a-menu-item key="departments">部门列表</a-menu-item>
  </a-sub-menu>

  <!-- 所有角色可见 -->
  <a-sub-menu key="att-sub">
    <template #title>考勤管理</template>
    <a-menu-item key="attendance">考勤记录</a-menu-item>
  </a-sub-menu>

  <!-- 仅管理员可见 -->
  <a-sub-menu v-if="authStore.isAdmin" key="user-sub">
    <template #icon><SafetyCertificateOutlined /></template>
    <template #title>账号管理</template>
    <a-menu-item key="users" @click="$router.push('/users')">账号列表</a-menu-item>
    <a-menu-item key="user-add" @click="$router.push('/users/add')">添加账号</a-menu-item>
  </a-sub-menu>
</a-menu>
```

#### 4. API请求/响应拦截器

**文件：** `api/auth.js` 和 `api/employee.js`

```javascript
const api = axios.create({
  baseURL: '/api',
  timeout: 10000
})

// 请求拦截器 - 自动携带Token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器 - 处理错误
api.interceptors.response.use(
  response => response,

  error => {
    if (error.response?.status === 401) {
      authStore.logout()                          // 清除状态
      message.error('登录已过期，请重新登录')       // 提示用户
      router.push('/login')                       // 跳转登录
    } else if (error.response?.status === 403) {
      message.error('没有权限访问此功能')
    }
    return Promise.reject(error)
  }
)
```

#### 5. 用户管理页面

**UserList.vue - 用户列表页功能：**

| 功能 | 说明 |
|------|------|
| 分页展示 | 支持分页、每页条数切换 |
| 搜索过滤 | 支持用户名/姓名/角色搜索 |
| 编辑操作 | 跳转编辑页面修改信息 |
| 删除操作 | 保护管理员账号不可删除 |
| 重置密码 | 一键重置为123456 |
| 角色标签 | 管理员👑红色 / 员工👤蓝色 |
| 头像颜色 | 根据用户名首字母生成颜色 |

**UserForm.vue - 用户新增/编辑表单：**

| 功能 | 说明 |
|------|------|
| 表单验证 | 用户名3-20字符、密码最少6位、必填项校验 |
| 编辑模式 | 用户名禁用修改、密码留空则不修改 |
| 角色选择 | 下拉选择admin/employee |
| 双模式 | 根据`isEdit`判断是新增还是编辑 |

#### 6. User Store状态管理

**文件：** `store/user.js`

```javascript
import { defineStore } from 'pinia'
import { userApi } from '../api/user'  // 使用对象调用方式

export const useUserStore = defineStore('userManage', {
  state: () => ({
    users: { records: [], total: 0, current: 1, size: 10 },
    loading: false
  }),

  actions: {
    async fetchUsers(params) {
      this.loading = true
      const res = await userApi.getUserList(params)
      this.users = res.data.data
      this.loading = false
    },

    async createUser(userData) {
      await userApi.addUser(userData)
      await this.fetchUsers({ page: 1 })
    },

    async editUser(id, userData) {
      await userApi.updateUser(id, userData)
      await this.fetchUsers({ page: this.users.current })
    },

    async removeUser(id) {
      await userApi.deleteUser(id)
      await this.fetchUsers({ page: this.users.current })
    }
  }
})
```

### 一键启动脚本

**文件：** `start-backend.ps1`

```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_201"
$port = 8080

# 自动检测并终止占用端口的进程
$processId = (Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue).OwningProcess
if ($processId) {
    Write-Host ">>> Port $port is in use by PID $processId, killing..." -ForegroundColor Yellow
    taskkill /F /PID $processId | Out-Null
    Start-Sleep -Seconds 2
    Write-Host ">>> Port $port released" -ForegroundColor Green
}

# 启动Spring Boot
cd d:\xiangmu\cainiao\employee-system\backend
mvn spring-boot:run
```

**使用方法：**
```bash
# 方式1: 双击脚本文件
start-backend.ps1

# 方式2: PowerShell执行
powershell -ExecutionPolicy Bypass -File start-backend.ps1
```

---

## 🎯 项目功能总览

### 已完成功能模块

| 模块 | 功能点 | 状态 |
|------|--------|------|
| **用户认证** | 登录/登出、JWT Token | ✅ 完成 |
| **员工管理** | CRUD、分页、搜索 | ✅ 完成 |
| **部门管理** | CRUD | ✅ 完成 |
| **考勤管理** | 签到/签退、状态判断 | ✅ 完成 |
| **公告通知** | 发布/编辑/删除 | ✅ 完成 |
| **请假管理** | 申请/审批流程 | ✅ 完成 |
| **账号管理** | 用户CRUD、角色分配、密码重置 | ✅ 完成 |
| **权限控制** | RBAC角色权限、菜单显隐、路由守卫 | ✅ 完成 |
| **安全机制** | BCrypt密码加密、JWT认证、401自动跳转 | ✅ 完成 |
| **便捷功能** | 自动释放端口、一键启动脚本 | ✅ 完成 |

### API接口汇总

| 模块 | 接口数量 | 主要接口 |
|------|----------|----------|
| 认证 | 2 | /auth/login |
| 员工 | 5 | /employees CRUD |
| 部门 | 4 | /departments CRUD |
| 考勤 | 3 | /attendance, /checkin, /checkout |
| 公告 | 4 | /announcements CRUD |
| 请假 | 5 | /leaves CRUD + approve/reject |
| 用户 | 6 | /users CRUD + reset-password |
| **总计** | **29** | - |

---

## 💡 开发经验总结

### 1. MyBatis Plus使用要点
- Service层必须继承`IService<Entity>`才能使用内置CRUD方法
- 使用`QueryWrapper`构建复杂查询条件
- 分页需配合`PaginationInnerInterceptor`插件

### 2. Vue3组件开发规范
- 使用Composition API(`<script setup>`)
- API导入使用对象形式：`import { userApi } from '../api/user'`
- 调用方式：`userApi.getUserById()` 而非解构单独函数
- computed属性只读，不要用v-model绑定

### 3. 前后端联调注意事项
- 所有API必须通过拦截器携带Token
- 后端返回统一格式`{code, message, data}`
- 401错误需在前端拦截器统一处理(清除状态+跳转登录)
- 浏览器缓存问题可通过重启dev server解决

### 4. 权限控制最佳实践
- 后端：Security配置白名单 + Controller注解
- 前端：Router meta.roles + v-if条件渲染
- 存储：localStorage持久化角色信息
- 拦截器：401自动退出，403提示无权限

### 5. 开发效率提升技巧
- 自动释放端口避免手动终止进程
- 一键启动脚本简化操作流程
- HMR热更新加快开发调试
- Vite快速冷启动(< 2秒)

---

## 🚀 快速启动指南

### 环境要求
- JDK 1.8.0_201+
- Node.js 16+
- MySQL 8.0+

### 启动步骤

```bash
# 1. 启动后端(自动释放端口)
powershell -ExecutionPolicy Bypass -File start-backend.ps1

# 2. 启动前端
cd frontend
npm run dev

# 3. 访问系统
# 前端地址: http://localhost:3003
# 后端地址: http://localhost:8080
# Swagger文档: http://localhost:8080/swagger-ui.html
```

### 测试账号
- **管理员：** admin / admin123 （可见所有菜单）
- **普通员工：** 可自行添加（仅可见部分菜单）

---

*学习日报最后更新时间：2026年06月01日*
*项目版本：v1.0.0 - 完整版*
